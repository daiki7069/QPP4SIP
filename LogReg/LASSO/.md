L1正則化付きロジスティック回帰を行います。
#入力
- ベースモデルからのスコア(例: /home/daiki_shibata/pj/QPP4SIP/SIP/FT-PLM/output/bert-base_lr2e-05_bs16_kfold5/train_with_predictions.jsonのlogit_clarification)
- QPPスコア(例: /home/daiki_shibata/pj/QPP4SIP/QPP/post_retrieval/main.pyで計算されるスコアたち)

#正解データ
- /home/daiki_shibata/pj/QPP4SIP/dataset/INSCIT/train.json のresponse_type([SEP]で分けられた場合は前方を採用)がclarificationを正例とする

#前処理
- 各スコア(baseスコアとQPPスコアの全て)をそれぞれでz-score正規化する

#注意
- 訓練データのファイル名はtrainがついている方
- テストデータのファイル名はdevがついている方
- z-score正規化パラメーターµ,sigmaは訓練時のものをdevで使用(リーク防止)